import { ProgressBar } from "@tremor/react";
import type { RegistrationDetails } from "@/lib/organizer/registrations/registration-types";
import { formatRegistrationDate } from "@/lib/organizer/registrations/registration-formatters";
import { RegistrationStatusBadge } from "./registration-status-badge"; import { EligibilityBadge } from "./eligibility-badge";
export function RegistrationOverviewTab({ registration }: { registration: RegistrationDetails }) {
  return <div className="space-y-5"><div className="rounded-xl border border-border p-4"><div className="flex items-center gap-3"><span className="grid size-12 place-items-center rounded-full bg-orange-500/10 font-semibold text-orange-400">{registration.student.fullName.split(" ").map((word) => word[0]).slice(0, 2).join("")}</span><div><p className="font-semibold">{registration.student.fullName}</p><p className="text-xs text-muted-foreground">{registration.student.email} · {registration.student.studentId}</p></div></div><dl className="mt-4 grid gap-3 text-sm sm:grid-cols-2"><div><dt className="text-xs text-muted-foreground">Department</dt><dd>{registration.student.department}</dd></div><div><dt className="text-xs text-muted-foreground">School</dt><dd>{registration.student.school}</dd></div><div><dt className="text-xs text-muted-foreground">Phone</dt><dd>{registration.student.phone}</dd></div><div><dt className="text-xs text-muted-foreground">Profile completion</dt><dd>{registration.student.profileCompletion}%</dd></div></dl><ProgressBar value={registration.student.profileCompletion} color="orange" className="mt-3" /></div>
    <div className="rounded-xl border border-border p-4"><h4 className="font-semibold">Registration information</h4><dl className="mt-4 grid gap-4 text-sm sm:grid-cols-2"><div><dt className="text-xs text-muted-foreground">Event</dt><dd>{registration.event.name}</dd></div><div><dt className="text-xs text-muted-foreground">Track</dt><dd>{registration.event.track}</dd></div><div><dt className="text-xs text-muted-foreground">Registered at</dt><dd>{formatRegistrationDate(registration.registeredAt)}</dd></div><div><dt className="text-xs text-muted-foreground">Source</dt><dd>{registration.source}</dd></div><div><dt className="text-xs text-muted-foreground">Status</dt><dd className="mt-1"><RegistrationStatusBadge status={registration.status} /></dd></div><div><dt className="text-xs text-muted-foreground">Eligibility</dt><dd className="mt-1"><EligibilityBadge status={registration.eligibility} reason={registration.eligibilityReason} /></dd></div></dl></div></div>;
}
