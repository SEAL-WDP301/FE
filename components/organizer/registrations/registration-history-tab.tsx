import type { RegistrationHistoryItem } from "@/lib/organizer/registrations/registration-types";
export function RegistrationHistoryTab({ history }: { history: RegistrationHistoryItem[] }) { return <ol className="relative ml-2 border-l border-border pl-5">{history.map((item) => <li key={item.id} className="relative pb-6 last:pb-0"><span className="absolute -left-[25px] top-1 size-2 rounded-full bg-orange-400 ring-4 ring-background" /><p className="text-sm font-semibold">{item.action}</p><p className="mt-1 text-xs text-muted-foreground">{item.actor} · {item.time}</p>{item.note && <p className="mt-2 rounded-lg bg-muted/40 p-2 text-xs">{item.note}</p>}</li>)}</ol>; }
