"use client";
import { Sheet, SheetContent, SheetDescription, SheetTitle } from "@/components/ui/sheet";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import type { RegistrationDetails } from "@/lib/organizer/registrations/registration-types";
import { RegistrationOverviewTab } from "./registration-overview-tab"; import { RegistrationAnswersTab } from "./registration-answers-tab"; import { RegistrationTeamTab } from "./registration-team-tab"; import { RegistrationHistoryTab } from "./registration-history-tab";
export function RegistrationDetailsDrawer({ registration, open, onOpenChange }: { registration: RegistrationDetails | null; open: boolean; onOpenChange: (open: boolean) => void }) {
  return <Sheet open={open} onOpenChange={onOpenChange}><SheetContent side="right" className="w-full gap-0 overflow-y-auto p-5 sm:max-w-2xl"><SheetTitle>Registration details</SheetTitle><SheetDescription className="mt-1">{registration?.id ?? "Loading registration..."}</SheetDescription>{registration && <Tabs defaultValue="overview" className="mt-6"><TabsList className="w-full overflow-x-auto"><TabsTrigger value="overview">Overview</TabsTrigger><TabsTrigger value="answers">Answers</TabsTrigger><TabsTrigger value="team">Team</TabsTrigger><TabsTrigger value="history">History</TabsTrigger></TabsList><TabsContent value="overview" className="mt-5"><RegistrationOverviewTab registration={registration} /></TabsContent><TabsContent value="answers" className="mt-5"><RegistrationAnswersTab answers={registration.answers} /></TabsContent><TabsContent value="team" className="mt-5"><RegistrationTeamTab team={registration.team} /></TabsContent><TabsContent value="history" className="mt-5"><RegistrationHistoryTab history={registration.history} /></TabsContent></Tabs>}</SheetContent></Sheet>;
}
